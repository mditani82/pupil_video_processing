# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /Users/moeitani/Desktop/yolo/data # dataset root dir
train: 
  - images/train_watch  # train images (relative to 'path') 128 images
  - images/train_pods
val: 
  - images/test_watch
  - images/test_pods  # val images (relative to 'path') 128 images
test:  # test images (optional)

# Classes (80 COCO classes)
names:
  0: pods
  1: watch
